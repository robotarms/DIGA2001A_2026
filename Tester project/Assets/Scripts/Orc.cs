using UnityEngine;

public class Orc : Enemy
{
    public void Roar()
    {
        Debug.Log("GRRRAAAAWWWR!!!");
    }
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
